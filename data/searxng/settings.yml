use_default_settings: true

general:
  instance_name: "LIU-RAG-AI Search"

server:
  secret_key: "junjie-rag-secret"
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false
  image_proxy: false

search:
  formats:
    - json
    - html

outgoing:
  request_timeout: 10.0
  max_request_timeout: 15.0
