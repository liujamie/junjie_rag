# Docker 环境配置 — 覆盖连接地址为容器名
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/junjie_rag?serverTimezone=UTC&socketTimeout=600000&connectTimeout=30000

  data:
    redis:
      host: redis

  ai:
    websearch:
      searxng:
        url: "http://searxng:8080/search"
    vectorstore:
      milvus:
        client:
          host: milvus
